# Backend

-- Specific backend for the messagin app Yui

Features:
    1. Authorization - Basically a register and login system as well as a authorization mechanism. A middleware that is even after every request that is made to the server to check if the user is authenticated, through this there is a check for a specific object within the request object to validate user auth

    2. Has multiple endpoints to handle different factors will list them down later on.
