async function getChat(req, res, next) {
				// TODO: Get user chat with the specific user specified in the userId param.
}

async function sendMessage(req, res, next) {
				//TODO: Send message to user with the userId param as the receiver.
}

module.exports = {
				getChat,
				sendMessage
}
